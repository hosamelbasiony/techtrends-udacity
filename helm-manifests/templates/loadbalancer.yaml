apiVersion: v1
kind: Service
metadata:
  name: loadbalancer-service
  namespace: {{ .Values.namespace.name }}
spec:
  selector:
    app: techtrends
    tag: udacity
  ports:
    - port: {{ .Values.replicaCount }}
      targetPort: 3111
  type: LoadBalancer